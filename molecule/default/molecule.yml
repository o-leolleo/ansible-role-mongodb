---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
platforms:
  - name: Ubuntu
    image: leolleocompo/ubuntu:18.04
    # command: /sbin/init
    privileged: True
provisioner:
  name: ansible
verifier:
  name: ansible
